### Backend - Library Management System

- Spring Boot 3, JPA, Security (JWT), Flyway, Springdoc (Swagger)
- H2 (dev) / PostgreSQL (prod)
- Run: `mvn spring-boot:run`
- Swagger UI: `/swagger-ui.html`
